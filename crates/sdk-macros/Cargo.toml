[package]
name = "hyperscale-vm-sdk-macros"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The `#[blueprint]` macro: contract bodies lowered to effect declarations"

[lib]
proc-macro = true

[dependencies]
# The role vocabulary and the band a package's own roles clear, read
# rather than restated: the macro validates against the same constants an
# engine derives keys from.
hyperscale-vm-effects.workspace = true
proc-macro2.workspace = true
quote.workspace = true
syn.workspace = true

[lints]
workspace = true
