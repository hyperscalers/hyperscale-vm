error: this key is not derivable from the method's arguments or the component's configuration. Routing evaluates a declaration before execution and never reads state, so a key computed from a substate value cannot be declared — pass it as a parameter instead
  --> tests/refusals/reassigned_key.rs:20:28
   |
20 |             self.vaults.at(key).add(0);
   |                            ^^^
