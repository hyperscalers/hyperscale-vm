error: a package's own reserves are declared: a `Vault` field under `#[holds(..)]`, or `Keyed<Vault>` for an open family — `self.vault()` reads the protocol balance, which is the principals blueprint's
  --> tests/refusals/two_denominations.rs:22:13
   |
22 |             self.vault(settings.base).put(half);
   |             ^^^^

error: a package's own reserves are declared: a `Vault` field under `#[holds(..)]`, or `Keyed<Vault>` for an open family — `self.vault()` reads the protocol balance, which is the principals blueprint's
  --> tests/refusals/two_denominations.rs:23:13
   |
23 |             self.vault(settings.quote).put(rest);
   |             ^^^^
