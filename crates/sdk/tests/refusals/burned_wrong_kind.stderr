error: this edge carries an amount, and an instance of `OwnerBadge` is what the call is about — a non-fungible edge is an `NfBucket`, which names the instances it moves
  --> tests/refusals/burned_wrong_kind.rs:17:30
   |
17 |             OwnerBadge::burn(funds);
   |                              ^^^^^
