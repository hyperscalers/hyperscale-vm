error: this merges `issued(Unit)` into `config.asset`, and one edge carries one resource. A merge moves value without converting it, so the two have to be the same resource
  --> tests/refusals/merged_across.rs:25:13
   |
25 |             taken.put(mint(Unit, amount));
   |             ^^^^^
