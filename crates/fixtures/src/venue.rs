//! The pass sold at the door: a component proving conditionally.
//!
//! What the corpus exercises through it is `#[proves(self)]` with an
//! ordinary body — payment banked against a configured price, short
//! payments declined — and the proof spent where another instance's
//! configuration names the prover.

guest!(venue, "../../../guests/venue/src/lib.rs");
