state
     16  till — one leaf, holding u128, denominated in config.asset
config
      0  asset
resources
  fungible["debt"] — granting mint to the issuer and burn to the issuer and deposit to nobody
errors
      0  short

draw(u128) — infallible
  mints    "debt" fungible
  declares
      0  debit self.till[config.asset]
  yields   edge 0 carrying config.asset
  yields   edge 1 carrying self-issued(fungible["debt"])
  exports  handle for clause 0, arg0

instantiate() — infallible
  declares
      0  requires self.config is absent
      1  write self.config
      2  requires self.resource[self-issued(fungible["debt"])] is absent
      3  write self.resource[self-issued(fungible["debt"])]
  exports  handle for clause 1, handle for clause 3, self.record

repay(bucket, bucket) — fallible
  takes    arg0 carrying config.asset
  takes    arg1 carrying self-issued(fungible["debt"])
  burns    "debt" fungible
  declares
      0  credit self.till[config.asset]
  exports  handle for clause 0, edge arg0, edge arg1
