config
      0  asset
errors
      0  short

draw(u128) — infallible
  mints    "debt" fungible, granting mint to the issuer and burn to the issuer and deposit to nobody
  declares
      0  delta self.vault[config.asset] holding config.asset
  yields   edge 0 = config.asset
  yields   edge 1 = self-issued(fungible["debt"]), granting mint to the issuer and burn to the issuer and deposit to nobody
  exports  handle for clause 0, arg0

instantiate() — infallible
  declares
      0  requires self.config is absent
      1  write self.config
      2  requires self.resource[self-issued(fungible["debt"]), granting mint to the issuer and burn to the issuer and deposit to nobody] is absent
      3  write self.resource[self-issued(fungible["debt"]), granting mint to the issuer and burn to the issuer and deposit to nobody]
  exports  handle for clause 1, handle for clause 3, self.record

repay(bucket, bucket) — fallible
  takes    arg0 carrying config.asset
  takes    arg1 carrying self-issued(fungible["debt"]), granting mint to the issuer and burn to the issuer and deposit to nobody
  burns    "debt" fungible, granting mint to the issuer and burn to the issuer and deposit to nobody
  declares
      0  credit self.vault[config.asset] holding config.asset
  exports  handle for clause 0, edge arg0, edge arg1
