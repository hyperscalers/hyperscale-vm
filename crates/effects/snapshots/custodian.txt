state
     16  till — one leaf, holding u128, denominated in config.asset
     17  other_till — one leaf, holding u128, denominated in config.other
     18  locker — an ordered collection, holding nothing, denominated in config.instances
config
      0  asset
      1  other
      2  instances

churn(bucket, u128) — infallible
  takes    arg0 carrying config.asset
  declares
      0  delta self.till[config.asset]
  yields   edge 0 carrying config.asset
  exports  handle for clause 0, edge arg0, arg1

deposit(bucket) — infallible
  takes    arg0 carrying config.asset
  declares
      0  credit self.till[config.asset]
  exports  handle for clause 0, edge arg0

file(nf-bucket) — infallible
  takes    arg0 carrying config.instances
  declares
      0  file self.locker[config.instances] over the whole order space, at most len(ids-of(arg0)) entries
  exports  handle for clause 0, edge arg0

instantiate() — infallible
  declares
      0  requires self.config is absent
      1  write self.config
  exports  handle for clause 1, self.record

release(ids) — infallible
  declares
      0  take self.locker[config.instances] over the whole order space, at most len(arg0) entries
  yields   edge 0 carrying config.instances × arg0
  exports  handle for clause 0, arg0

swap(bucket, u128) — infallible
  takes    arg0 carrying config.asset
  declares
      0  credit self.till[config.asset]
      1  debit self.other_till[config.other]
  yields   edge 0 carrying config.other
  exports  handle for clause 0, handle for clause 1, edge arg0, arg1

withdraw(u128) — infallible
  declares
      0  debit self.till[config.asset]
  yields   edge 0 carrying config.asset
  exports  handle for clause 0, arg0
