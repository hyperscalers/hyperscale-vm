state
     16  asks — an ordered collection, holding u128
config
      0  base
      1  quote
errors
      0  unpriced-ask

fill-asks(u64, u64, bucket) — infallible
  takes    arg2 carrying config.quote
  declares
      0  write self.asks over pack(arg0, 0)..=pack(arg1, u64::MAX), at most 64 entries
      1  delta self.vault[config.base] holding config.base
      2  delta self.vault[config.quote] holding config.quote
  yields   edge 0 = config.base
  yields   edge 1 = resource-of(arg2)
  exports  handle for clause 0, handle for clause 1, handle for clause 2, edge arg2

instantiate() — infallible
  declares
      0  requires self.config is absent
      1  write self.config
  exports  handle for clause 1, self.record

place-ask(u64, bucket) — fallible
  takes    arg1 carrying config.base
  declares
      0  write self.asks at pack(arg0, fresh-id(0))
      1  delta self.vault[config.base] holding config.base
  exports  handle for clause 0, handle for clause 1, arg0, fresh-id(0), edge arg1
