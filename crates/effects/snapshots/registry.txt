state
     16  names — an unordered collection, holding u128

bind(u64, u128) — infallible
  declares
      0  write self.names at order-key(self.names[arg0])
  exports  handle for clause 0, order-key(self.names[arg0]), arg1

check(u64, u128) — infallible
  declares
      0  read self.names at order-key(self.names[arg0])
  exports  handle for clause 0, arg1

drain(u128) — infallible
  declares
      0  write self.names over arg0..=u128::MAX, at most 8 entries
  exports  handle for clause 0
