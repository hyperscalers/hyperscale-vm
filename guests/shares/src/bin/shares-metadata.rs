//! Print this package's declaration as its canonical section bytes.
//! `cargo hyperscale build` runs this and attaches what it prints to the
//! code beside it.

hyperscale_vm_sdk::declaration_main!(shares_guest::shares);
